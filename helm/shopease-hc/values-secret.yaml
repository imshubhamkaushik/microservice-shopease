# DO NOT COMMIT REAL SECRETS HERE.
# This file ONLY documents the structure.

postgres:
  host: postgres
  port: 5432
  database: shopease
  username: shopease
  # password: ""   # NEVER store real password in Git!

# Recommended workflow:
# In CI:
# kubectl create secret generic shopease-secrets \
#   --from-literal=DB_PASSWORD="$DB_PASSWORD" \
#   -n your-namespace \
#   --dry-run=client -o yaml | kubectl apply -f -
#
# Then in Deployment templates:
#
# env:
#   - name: SPRING_DATASOURCE_PASSWORD
#     valueFrom:
#       secretKeyRef:
#         name: postgres-secret
#         key: DB_PASSWORD
